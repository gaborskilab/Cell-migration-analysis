function [displacement, disp_x, disp_y] = tot_disp(trajectory)% This function calculates net displacement from the beginning of a trajectory% to the end% if size(trajectory,2) ~= 2% 	disp('The trajectory must have data in columns!')% 	return% endx1 = trajectory(1,1);y1 = trajectory(1,2);x2 = trajectory(end,1);y2 = trajectory(end,2);displacement = sqrt((x1-x2)^2 + (y1-y2)^2);disp_x = x2 - x1; % negative values means moving from right to leftdisp_y = y2 - y1; % negative values means moving from bottom to top return
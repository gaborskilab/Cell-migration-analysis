function p_ind_guess = p_ind(trajectory)% This function estimates persistence index (jim's way) as the displacement/path length% if size(trajectory,2) ~= 2% 	disp('The trajectory must have data in columns!')% 	return% endlength_p = p_length(trajectory); % calculates path length of trajectorydisp_traj = tot_disp(trajectory); % calculates total displacement of cellp_ind_guess = disp_traj/length_p;return